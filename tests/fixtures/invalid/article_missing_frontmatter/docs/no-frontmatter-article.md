# Artikel ohne Frontmatter

Dieser Artikel beginnt nicht mit einem YAML-Frontmatter-Block (`---`). Er liegt ausserhalb der
dokumentierten Ausnahmen (docs/project/**, index.md, tags.md) und muss vom Validator als Fehler erkannt
werden, nicht still ignoriert.
